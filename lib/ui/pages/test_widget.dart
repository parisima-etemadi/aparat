import 'package:flutter/material.dart';
class TestWidgetPrac extends StatelessWidget {
  const TestWidgetPrac({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Scaffold(body: Container(color: Colors.amber,),));
  }
}
